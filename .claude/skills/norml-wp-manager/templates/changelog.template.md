# {SITE_NAME} — WP management changelog

> Chronological log of every significant operation, decision, and
> learning on this site through norml-wp-manager. Newest entry on top.
> Append-only — Claude writes a new line before closing any response
> that mutated the site or made a decision worth remembering.

**Tags**:

- `[WRITE]` — content / postmeta change (post, page, ACF field value)
- `[OPTION]` — `wp_options` or `/wp/v2/settings` change
- `[USER]` — user create / role change / delete / password reset
- `[MEDIA]` — upload / attach / metadata update / delete
- `[DECISION]` — policy / convention agreed with the user
- `[LEARNED]` — non-obvious fact discovered (also written to `project-notes.md`)
- `[SCAN]` — architecture scan ran; notable changes since the last one
- `[ROLLBACK]` — explicit undo of a previous write

---

## {TODAY} — Initial setup

- {TIME} — [SCAN] First-time site architecture scan completed.
  Snapshot written to `site-architecture.md`. See that file for the
  full inventory.
